# Self Protocol Configuration
# Copy this file to .env.local and update the values

# Self Protocol App Configuration
VITE_SELF_APP_NAME=Credora
VITE_SELF_SCOPE=credora-identity
VITE_SELF_ENDPOINT=https://staging.self.xyz/api/verify

# Backend Configuration
VITE_SELF_BACKEND_ENDPOINT=http://localhost:3001

# Optional: For production deployment
# VITE_SELF_BACKEND_ENDPOINT=https://your-backend-domain.com

# Note: Make sure your scope is no longer than 30 characters
# The disclosures configuration must match between frontend and backend
